{
  "name": "default",
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "postgres",
  "database": "travelfinder",
  "synchronize": false,
  "logging": false,
  "entities": ["/models/**/*.ts"],
  "migrations": ["/migrations/**/*.ts"],
  "cli": {
    "entitiesDir": "/models",
    "migrationsDir": "/migrations"
  }
}

